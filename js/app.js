let data;const $=s=>document.querySelector(s);async function load(){data=await fetch('data.json').then(r=>r.json());const local=JSON.parse(localStorage.getItem('osisvote-data')||'null');if(local&&local.tokens&&local.votes)data=local;render()}function save(){localStorage.setItem('osisvote-data',JSON.stringify(data))}function render(){document.getElementById('statusText').textContent=data.electionOpen?'Voting dibuka':'Voting ditutup';$('#candidateGrid').innerHTML=data.candidates.map((c,i)=>`<article class="candidate card" data-id="${c.id}"><div class="avatar">${c.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div><span class="badge">CALON ${i+1}</span><h3>${c.name}</h3><small>${c.class}</small><p>${c.vision}</p></article>`).join('');document.querySelectorAll('.candidate').forEach(el=>el.onclick=()=>choose(el.dataset.id));}function choose(id){$('#candidateGrid').querySelectorAll('.candidate').forEach(x=>x.style.outline=x.dataset.id===id?'2px solid #5eead4':'none');$('#voteForm').dataset.candidate=id;document.getElementById('vote').scrollIntoView({behavior:'smooth'})}async function init(){await load();$('#voteForm').onsubmit=async e=>{e.preventDefault();if(!data.electionOpen)return msg('Voting sedang ditutup.');const t=$('#token').value.trim();const h=await sha256(t.toUpperCase());const tok=data.tokens.find(x=>x.hash===h);if(!tok)return msg('Token tidak valid.');if(tok.used)return msg('Token sudah digunakan.');const candidate=$('#voteForm').dataset.candidate;if(!candidate)return msg('Pilih kandidat terlebih dahulu.');tok.used=true;tok.usedAt=new Date().toISOString();data.votes.push({candidateId:candidate,time:new Date().toISOString()});data.audit.push({type:'vote',time:new Date().toISOString()});save();msg('Suara berhasil direkam. Token ini tidak dapat digunakan lagi.');$('#voteForm').reset();};}function msg(x){$('#msg').textContent=x}window.addEventListener('storage',load);init();
